#include <bits/stdc++.h>
using namespace std;

int main() {
    // Everyone uses those two lines by default. Safe and faster for CF.
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    int result = n*n;
    cout << result << endl';  // THIS is the answer CF checks
    
    // dont return non-zero or test will fail on CF
    return 0;
}

